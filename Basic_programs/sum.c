#include <stdio.h>
int main() 
{
	int sum, num2, num1;

	printf("Enter the first number: ");
	scanf("%d", &num1);
	printf("Enter the second character: ");
	scanf("%d", &num2);

	sum = num1 + num2;
	printf("The sum of your intergers are: %d\n", sum);

	return (0);
}
